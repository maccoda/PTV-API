package ptvobjects;

/**
 * An object that is obtained from PTV request which can be represented by a basic data type.
 * That is, is can be directly mapped from the JSON data to the type.
 */
public interface PtvBasicObject extends PtvObject {
}
